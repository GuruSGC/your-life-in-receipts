## What changed

<!-- One or two sentences. -->

## Checklist

- [ ] `npm run lint` passes with no warnings
- [ ] `npm run typecheck` and `npm run format:check` pass
- [ ] `npm run coverage` passes and the thresholds hold
- [ ] New findings list their evidence days and have an independent test
- [ ] Motion uses only `transform` and `opacity` and respects reduced motion
